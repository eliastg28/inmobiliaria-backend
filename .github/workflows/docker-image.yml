name: Build and Push Backend Docker Image

on:
  pull_request:
    branches:
      - main
    types:
      - closed
  workflow_dispatch:
    inputs:
      branch:
        description: 'Rama desde la cual desplegar (manual)'
        required: true
        default: 'main'
        type: choice
        options:
          - main
          - develop

jobs:
  build-and-push:
    # Condiciones:
    # - PR mergeado de develop -> main
    # - O ejecución manual
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.merged == true &&
       github.event.pull_request.base.ref == 'main' &&
       github.event.pull_request.head.ref == 'develop')

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || 'main' }}

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Build JAR with Maven
        run: mvn clean package -DskipTests

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/inmobiliaria-backend:${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || 'main' }}
